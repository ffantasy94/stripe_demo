{
  "name": "Stripe PHP example",
  "description": "Example Stripe integration with PHP backend",
  "repository": "https://github.com/ob-stripe/example-stripe-php-ideal",
  "keywords": ["stripe", "php", "sources"],
  "env": {
    "STRIPE_TEST_SECRET_KEY": {
      "description": "Stripe test secret key -- find this at https://dashboard.stripe.com/account/apikeys (it'll look like sk_test_****)",
      "required": true
    }
  }
}
